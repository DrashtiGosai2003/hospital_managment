<?xml version="1.0"?>
<odoo>
       <record id="patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form string="patient_view">
                <sheet>
                    <group>
                        <field name="ref"/>
                        <field name="partner_id"/>
                        <field name="name"/>
                        <field name="dob"/>
                        <field name="age"/>
                        <field name="age_group"/>
                        <field name="mobile_no"/>
                        <field name="gender"/>
                        <field name="email_id"/>
                         <field name="doctor_ids"/>
                    </group>
                </sheet>
            </form>
        </field>
       </record>

       <record id="view_patient_list" model="ir.ui.view">
        <field name="name">hospital.patient.list</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <list string="Patients">
                <field name="ref"/>
                <field name="name"/>
                <field name="age"/>
                <field name="gender"/>
                 <field name="email_id"/>
                <field name="doctor_ids"/>
            </list>
        </field>
    </record>

     <record id="patient_record" model="ir.actions.act_window">
        <field name="name">Pateint Details</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem
        id="patient_menu"
        name="Hospital_Mangament"
        />
     <menuitem
        id="model_name_menu"
        name="Patient"
        action="patient_record"
        parent="patient_menu"
        sequence="10"/>
    
  
</odoo>